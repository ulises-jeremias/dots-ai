# CLI Helpers

- `dots-bootstrap`: bootstrap guidance and optional apply.
- `dots-sync-ai`: validates AI asset directories.
- `dots-doctor`: checks baseline compliance.
- `dots-update-check`: checks whether local baseline is outdated.
