# AI Agent Instructions

Project: {{ .company.name }}
Repository type: Internal workstation platform

## Guardrails

- Do not commit credentials or tokens.
- Keep changes small, reversible, and documented.
- Prefer profile-driven behavior over machine-specific logic.
- Validate shell scripts before proposing merge.

## Conventions

- Use `nan-` prefix for custom CLI commands.
- Keep docs in uppercase names under `docs/`.
- Keep wiki docs under `docs/wiki/`.
