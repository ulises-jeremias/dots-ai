# Copilot Instructions Template

## Platform Scope

Work only within the dots-ai baseline and keep behavior profile-driven.

## Security Rules

- Never hardcode API keys.
- Use environment variables in examples.

## Documentation Rules

- Update `docs/` uppercase documents when behavior changes.
- Keep `docs/wiki/` and `docs/adrs/` synchronized with repository changes.
