# Skills Registry
#
# This file documents skills known to this baseline.
# It is used by `dots-skills` for npm-sourced skills (install + sync).
#
# Deployed to ~/.local/share/dots-ai/skills-registry.yaml via chezmoi.
# Keep in sync with home/.chezmoidata/skills-registry.yaml (same content).
#
# Skills from github/url sources are managed natively by chezmoi:
#   home/private_dot_local/share/dots-ai/.chezmoiexternal.toml.tmpl
#   Opt-in via chezmoidata install_skill_* flags.
#
# Field reference:
#   name:     Unique kebab-case identifier
#   source:   bundled | npm  (github/url → see .chezmoiexternal.toml.tmpl)
#   enabled:  Whether this skill is active by default

skills:
  # ── Bundled (distributed via chezmoi) ───────────────────────────────────────
  - name: clickup-cli
    source: bundled
    enabled: true

  - name: slack-cli
    source: bundled
    enabled: true

  - name: dots-ai-workstation-triage
    source: bundled
    enabled: true

  - name: dots-ai-assistant
    source: bundled
    enabled: true

  - name: github-cli-workflow
    source: bundled
    enabled: true

  - name: gitlab-cli-workflow
    source: bundled
    enabled: true

  - name: dbt-validation
    source: bundled
    enabled: true

  - name: snowflake-validation
    source: bundled
    enabled: true

  - name: dots-ai-workflow-generic-project
    source: bundled
    enabled: true

  - name: dots-ai-dev-companion
    source: bundled
    enabled: true

  - name: dots-ai-workflow-client-bootstrap
    source: bundled
    enabled: true

  # Deprecated stub dirs under skills/ — must NOT be managed by dots-skills sync
  # (chezmoi owns ~/.claude/skills/workflow-* → dots-ai-workflow-* canonical paths).
  - name: workflow-generic-project
    source: bundled
    enabled: false

  - name: workflow-client-bootstrap
    source: bundled
    enabled: false

  - name: dots-slack-assistant
    source: bundled
    enabled: true

  - name: dots-ai-workspace-knowledge-sync
    source: bundled
    enabled: true

  - name: dots-ai-prd
    source: bundled
    enabled: true

  - name: dots-ai-trd
    source: bundled
    enabled: true

  - name: dots-ai-adr
    source: bundled
    enabled: true

  - name: dots-ai-output-handshake
    source: bundled
    enabled: true

  - name: dots-ai-pr-fallback
    source: bundled
    enabled: true

  - name: dots-ai-planning
    source: bundled
    enabled: true

  - name: dots-ai-development-workflow
    source: bundled
    enabled: true

  - name: dots-ai-work-item
    source: bundled
    enabled: true

  - name: dots-ai-epic
    source: bundled
    enabled: true

  - name: dots-ai-user-story
    source: bundled
    enabled: true

  - name: dots-ai-task
    source: bundled
    enabled: true

  - name: dots-ai-bug
    source: bundled
    enabled: true

  - name: dots-ai-incident
    source: bundled
    enabled: true

  - name: dots-ai-meeting-minutes
    source: bundled
    enabled: true

  - name: dots-ai-decision-log
    source: bundled
    enabled: true

  - name: dots-ai-agreement
    source: bundled
    enabled: true

  - name: dots-ai-spike
    source: bundled
    enabled: true

  - name: dots-ai-project-assessment
    source: bundled
    enabled: true

  - name: dots-ai-project-assessment-evidence
    source: bundled
    enabled: true

  - name: dots-ai-technical-unit-assessment
    source: bundled
    enabled: true

  - name: dots-ai-management-unit-assessment
    source: bundled
    enabled: true

  # ── Imported from openai/skills (Apache-2.0) ────────────────────────────────
  # See docs/SKILLS.md → "Imported skills" and each skill's NOTICE.txt.
  - name: gh-address-comments
    source: bundled
    enabled: true

  - name: gh-fix-ci
    source: bundled
    enabled: true

  - name: linear
    source: bundled
    enabled: true

  - name: figma
    source: bundled
    enabled: true

  - name: figma-implement-design
    source: bundled
    enabled: true

  - name: figma-code-connect-components
    source: bundled
    enabled: true

  - name: figma-create-design-system-rules
    source: bundled
    enabled: true

  - name: figma-create-new-file
    source: bundled
    enabled: true

  - name: playwright-cli
    source: bundled
    enabled: true

  - name: jupyter-notebook
    source: bundled
    enabled: true

  # ── npm registry ─────────────────────────────────────────────────────────────
  - name: ui-ux-pro-max
    source: npm
    package: uipro-cli
    enabled: true

  # ── github/url skills → managed by .chezmoiexternal.toml.tmpl ────────────────
  # JIRA Assistant Skills (14 skills) — install_skill_jira_assistant = true
  # Confluence Assistant Skills (17 skills) — install_skill_confluence_assistant = true
  # See: home/private_dot_local/share/dots-ai/.chezmoiexternal.toml.tmpl
