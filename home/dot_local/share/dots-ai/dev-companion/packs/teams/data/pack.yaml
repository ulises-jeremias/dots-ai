kind: team
slug: data
display_name: Data Engineering

triggers:
  keywords:
    - dbt
    - Snowflake
    - warehouse

boundaries:
  allowed_paths:
    - ~/Projects/
  tool_requirements:
    - dbt

automation:
  automation_level_default: plan_only
  allow_background_runner: true
